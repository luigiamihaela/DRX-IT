export type UserProfileToken = {
    username: string;
    email: string;
    roles: string[];
    accessToken: string;
};

export type UserProfile = {
    username: string;
    email: string;
    roles: string[];
}